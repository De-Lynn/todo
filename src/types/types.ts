export type TaskType = {
    id: string,
    title: string,
    isDone: boolean
}

export type StyledFlexPropsType = {
    direction?: string,
    align?: string,
    justify?: string,
    margin?: string,
} 
export type StyledButtonPropsType = {
    fontSize?: string,
    height?: string,
    width?: string,
    margin?: string,
} 